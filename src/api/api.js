/**
 * 封装前端api请求
 */
const api = {
  /**
   *获取单词例句
   * @param {String} words 要查询例句的单词
   * @param {callback function} response 正常返回结果时的回调
   * @param {callback function} err 出错时的回调
   */
  getWordsExample(words, response, err) {

  }
};

export default api;